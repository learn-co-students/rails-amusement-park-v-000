<% @attractions.each do |att| %>
    <tr>
        <td><%= att.name %></td>
        <td><%= att.tickets %></td>
        <td><%= link_to "Go on #{att.name}", attraction_path(att) %></td>
    </tr>
<% end %>
