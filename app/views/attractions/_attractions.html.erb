<%=link_to 'New Attraction', new_attraction_path if User.find(session[:user_id]).admin %>

<%@attractions.each do |attraction| %>
<%=link_to "Go on #{attraction.name}", attraction_path(attraction) unless User.find(session[:user_id]).admin %>
<%=link_to "Show #{attraction.name}", attraction_path(attraction) if User.find(session[:user_id]).admin  %>
<%end%>
