<% @attractions.each do |a| %>

<% if  !@current_user.admin? %>
 <%= link_to "Go on #{a.name}", attraction_path(a.id) %><br>
 <% elsif @current_user.admin? %>
 <%= link_to "Show #{a.name}", attraction_path(a.id) %>
<% end %>
<% end %>


<%= link_to "New Attraction", new_attraction_path %>
