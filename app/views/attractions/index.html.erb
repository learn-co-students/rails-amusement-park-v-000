<% if authorized_user? %>
  <%= link_to "New Attraction", new_attraction_path %>
<% end %>

<br>
<ol>
<% @attractions.each do |attraction| %>
  <li><%= link_to "Go on #{attraction.name}", attraction_path(attraction) if !authorized_user? %></li>
  <li><%= link_to "Show #{attraction.name}", attraction_path(attraction) if authorized_user? %></li><br>
<% end %>
</ol>
