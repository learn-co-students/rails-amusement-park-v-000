<p id="notice"><%= notice %></p>

<h1>Attractions</h1><br>

<table class="table">
  <thead>
    <tr class="row">
      <th scope="col" class="col-4">Name</th>
      <th scope="col" class="text-center col-1">Tickets</th>
      <th scope="col" class="text-right col-4">Details</th>
    </tr>
  </thead>

  <tbody>
    <% @attractions.each do |attraction| %>
      <tr class="row">
        <td class="col-4"><%= attraction.name %></td>
        <td class="text-center col-1"><%= attraction.tickets %></td>
        <td class="text-right col-4"><%= link_to table_display(attraction), attraction %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Create New Attraction', new_attraction_path %>
