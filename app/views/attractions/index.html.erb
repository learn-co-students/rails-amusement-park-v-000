<% if current_user.admin%>
<%=link_to "New Attraction", new_attraction_path%>
<%end%>


<%@attractions.each do |t|%>
<%if current_user.admin == false%>
<%=link_to "Go on #{t.name}", attraction_path(t.id)%>
<%end%>
  <p>
    <%if current_user.admin%>
  <%= link_to "Show #{t.name}", attraction_path(t.id)%>
  <%end%>
  <%end%>
