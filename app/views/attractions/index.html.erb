<% if @user.admin == true || @user.admin == 1%>
    ADMIN<br>
<%= link_to 'New Attraction', new_attraction_path %>
    <% @attractions.each do |att| %>
      <%= link_to "Show #{att.name}", attraction_path(att) %><br>
    <% end %>
 <% else %>
 <% @attractions.each do |att| %>
  <%= link_to "Go on #{att.name}", attraction_path(att) %><br>
 <% end %>
<% end %>
