<h1><%= @attraction.name %></h1>

<h3>The following is a summary of this attraction's information...</h3>
<p>Tickets required to ride: <%= @attraction.tickets %></p>
<p>Nausea Rating: <%= @attraction.nausea_rating %></p>
<p>Happiness Rating: <%= @attraction.happiness_rating %></p>
<p>Minimum Height to Ride: <%= @attraction.min_height %></p>

<% if @user.admin == false %>
  <%= form_tag("/rides", method: "post") do %>
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <%= hidden_field_tag :attraction_id, @attraction.id %>
    <%= button_tag "Go on this ride" %>
  <% end %>
<% else @user.admin == true %>
  <p><%= link_to "Edit Attraction", edit_attraction_path(@attraction)%></p>

<%= @attraction.name %>

<%= button_to "Go on this ride", user_path, :method => "put" %>

 <% if User.find(session[:user_id]).admin %>
  <%= link_to "Edit Attraction", edit_attraction_path(@attraction) %>
<% end %>
