<h1><%= @attraction.name %></h1>

<%= button_to 'Go on this ride', {:controller => "rides", :action => "new", :user_id => session[:user_id], :attraction_id => @attraction.id} %>

<%= link_to "Edit Attraction", edit_attraction_path %>
