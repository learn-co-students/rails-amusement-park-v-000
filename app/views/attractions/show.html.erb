<h1>Attraction Show Page</h1>

<ul>
  <li>Name: <%= @attraction.name %></li>
  <li>Number of Tickets: <%= @attraction.tickets %></li>
  <li>Height Requirements: <%= @attraction.min_height %></li>
</ul>
<% if current_user.admin == false %>
    <%= form_tag('/rides/new', :method => :get) do %>
        <%= hidden_field_tag 'current_user', "#{current_user.id}" %>
        <%= hidden_field_tag '@attraction', "#{@attraction.id}" %>
        <%= button_tag "Go on this ride" %>
    <% end %>
<% else %>
  <%= link_to "Edit Attraction", edit_attraction_path(@attraction) %>
<% end %>