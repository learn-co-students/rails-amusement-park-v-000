<h1><%= @attraction.name %></h2>

<% if @user.admin %>
  <p><%= link_to "Edit Attraction", edit_attraction_path(@attraction) %></p>
<% else %>
  <%= button_to "Go on this ride", rides_path, method: :post, params: {attraction: @attraction} %>
<% end %>
