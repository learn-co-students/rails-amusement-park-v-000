<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="<%= root_url %>"/>Home</a>          
            <a class="navbar-brand" href="<%= user_path(@user.id) %> "><%=@user.name%>'s profile</a>
            <a class="navbar-brand" data-method=post href="<%=logout_path%>">Log Out</a> 
        </div>
    </div>
</div>

<label> Name: </label> <%= @attraction.name %> <br>  
<label> Total Number of Users: </label> <%= @attraction.name %> <br> 
<label> Ticket Requirement: </label> <%= @attraction.tickets %> <br> 
<label> Height Requirement: </label> <%= @attraction.min_height %> inches <br> 

<%= button_to "Go on this ride", attraction_path(@attraction), method: :patch %> 

