<h1><%= @attraction.name %></h1>

<% if current_user.admin %>
<%= link_to "Edit Attraction", edit_attraction_path %>
<% else %>
<%= button_to "Go on this ride", '/ride', method: :get, params: {'attraction' => @attraction, 'user' => @user} %>
<% end %>
