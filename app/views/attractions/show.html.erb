<%= @attraction.name %>

<p>
  <% if @user.admin %>
    <%= link_to 'Edit Attraction', edit_attraction_path %>
  <% else %>
    <%= form_for @ride do |f| %>
      <%= f.hidden_field :attraction_id %>
      <%= f.submit 'Go on this ride' %>
    <% end %>
  <% end %>
</p>
