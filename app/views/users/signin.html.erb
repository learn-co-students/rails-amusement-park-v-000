<h1> Signin Page </h1>

<%= form_for @user, url: '/sessions' do |f| %>
  Name: <%= f.collection_select :id, User.all, :id, :name %><br />

  Password: <%= f.password_field :password %><br />
  <br />
<%= f.submit "Sign In" %>
<% end %>
