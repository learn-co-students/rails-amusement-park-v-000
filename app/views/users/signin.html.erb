<%= form_tag do %>
<!-- <%= options_from_collection_for_select(User.all, :id, :name) %> -->
<%= collection_select(:user, :name, User.all, :name, :name, {:include_blank => 'Select User'}) %><br>
Password: <%= password_field_tag :password %>
<%= submit_tag ("Sign In") %>
<% end %>
