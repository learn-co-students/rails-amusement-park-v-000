<%= form_for :user, url: signin_path do |f| %>
  Name: <%= f.collection_select :name, @users, :id, :name %><br />
  Password: <%= f.password_field :password, 'id' => "password" %><br />
  <%= f.submit 'Sign In' %>
<% end %>
