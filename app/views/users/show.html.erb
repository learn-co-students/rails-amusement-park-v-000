 <p> This is the users show page, List result or tickets after the went on the ride</p>
<header class="header">
    
      <h1><%= "user: #{@user.name}" %></h1>

      <h3><%= "Heights: #{@user.height}" %></h3>

       <h3><%= "Moods: #{@user.mood}" %></h3>

       <h3><%= "Happiness: #{@user.happiness}" %></h3>

       <h3><%= "Tickets: #{@user.tickets}" %></h3>

      <% if @user.admin %>
           <h1> ADMIN </h1>
      <%end%>

      <%=link_to "Log Out", logout_path, :method => :delete %>

  <br>
      <h2><%= link_to "See attractions", attractions_path %></h2>
  </header>

  