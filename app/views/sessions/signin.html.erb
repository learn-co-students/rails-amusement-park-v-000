<%= form_for(:session, url: signin_path) do |f| %>
  <%= f.label :name %>
  <%= f.select :user_name, options_from_collection_for_select(User.all, :name, :name) %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "Log in" %>
<% end %>
