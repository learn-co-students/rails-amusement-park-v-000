<%= form_for :session do |f| %>
  <%= select("user", "name", User.all.collect{|u| u.name }.insert(0, 'Please select your username from this dropdown')) %>
  <%= f.password_field :password, id: 'password', placeholder: 'Password' %>
  <%= f.submit 'Sign In'%>
<% end %>
