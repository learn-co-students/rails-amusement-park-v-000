<%= form_for @user, url: '/users' do |f| %>
  Username: <%= f.collection_select :name, User.all, :name, :name %><br>
  Password: <%= f.password_field :password %>
  <%= f.submit "Sign In", class: 'btn btn-default' %>
<% end %>
