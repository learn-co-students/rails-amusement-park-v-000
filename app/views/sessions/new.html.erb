<%= form_tag(signin_path) do %>
	<%= text_field(:user, :name, :placeholder => "Name") %></br>
	<%= password_field(:user, :password, :placeholder => "Password") %></br>

	<%= submit_tag("Sign In") %>
<% end %>