<%= form_tag sessions_path do |f| %>

	<%= label_tag :user_name, "Name" %>
	<%= select_tag :user_name, options_for_select(@users.collect{ |u| u.name}) %>
	<br>
	<%= label_tag :password %>
	<%= password_field_tag :password %>
	<br>
	<%= submit_tag 'Sign In'%>

<% end %>