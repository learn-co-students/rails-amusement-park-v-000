<%= form_tag(sessions_path) do  %>

	Name: <%= collection_select(:user, :name, User.all, :name, :name, html_options = {id: :user_name}) %><br>
	Password: <%= password_field_tag :password %><br>

	<%= submit_tag "Sign In" %>
		
<% end %>
  