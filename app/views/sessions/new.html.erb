<%= form_tag("/signin") do%>
  <%=label_tag('user[name]', "Name")%>
  <%=select_tag'user[name]', options_from_collection_for_select(@users, "name", "name")%>

  <%=label_tag('password')%>
  <%=text_field_tag('password')%>

  <%=submit_tag('Sign In')%>
<%end%>
