<%= form_for @user, :url => url_for(:controller => 'sessions', :action => 'create') do |f| %>
  <%= f.label :name %>:
  <%= f.select(:name, User.pluck(:name)) %><br />

  <%= f.label :password %>
  <%= f.password_field :password, id: 'password' %><br />

  <%= f.submit "Sign In" %>
<% end %>
