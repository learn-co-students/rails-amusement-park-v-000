<h1>Sign In Here</h1>
<%= form_for :user do |f|%>
  <%= f.collection_select(:name, User.all, :id, :name) %> <br>
  <%= f.label :password %>
  <%= f.password_field :password, :id => "password" %> <br>
  <%= f.submit "Sign In" %>
<% end %>
