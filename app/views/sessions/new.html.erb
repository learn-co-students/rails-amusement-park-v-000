<%= form_for @user, html: { id: "password" } do |f| %>
  <%= f.label :name %>:
  <%= f.collection_select(:name, User.all, :id, :name) %><br />

  <%= f.label :password %>:
  <%= f.password_field :password %><br />

  <%= f.submit %>
<% end %>
