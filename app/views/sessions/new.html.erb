<!-- <h1>Sign in to Rails Amusement Park</h1>
<%=form_tag '/signin' %>
<%=select_tag :user_name, options_from_collection_for_select(User.all, :name, :name)%>
<%=password_field_tag :password%>
<%=submit_tag "Sign In" %> -->

<%= form_for @user, :url => url_for(:controller => 'sessions', :action => 'create') do |f| %>
    <%= f.label :name, "Select your name" %><br>
    <%= f.collection_select(:name, User.all, :name, :name, prompt: "Please select username") %>
     <%= f.label :password, "Enter your password" %><br>
    <%= f.password_field :password %>
     <%= f.submit "Sign In" %>
<% end %>
